VANTA.CLOUDS({
  el: "#background",
  mouseControls: false,
  touchControls: false,
  gyroControls: false,
  minHeight: 200.00,
  minWidth: 200.00,
})



// fetch('/md').then(res => res.text()).then(d => console.log(d))

// document.getElementById('content').innerHTML = DOMPurify.sanitize(marked.parse(`# Marked in the browser\n\nRendered by **marked**.`));
